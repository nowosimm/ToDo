export default function makeProjectDiv() {
    var content = document.createElement("div");
    // var projectComplete = document.createElement("button");
    // var projectName = document.createElement("div");
    // var projectDate = document.createElement("div");
    // var projectDelete = document.createElement("button");

    // content.appendChild(projectComplete);
    // content.appendChild(projectName);
    // content.appendChild(projectDate);
    // content.appendChild(projectDelete);

    return content;
}